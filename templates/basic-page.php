<?php
$content = page('body');
//if($page->hasChildren) $content .= $wb->renderNav($page->children, 0, 'summary');

// sidebar
// if($page->rootParent->hasChildren > 1) {
// 	$sidebar = renderNav($page->rootParent, 3);
	//$sidebar .= ($page->sidebar)? $page->sidebar: $settings->sidebar;
// }

